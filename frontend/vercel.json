{
  "buildCommand": "npx ng build --configuration production --no-server-rendering",
  "outputDirectory": "dist/frontend/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}